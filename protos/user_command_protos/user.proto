syntax = "proto3";

package job;

service Job {
    rpc SendJob (JobMetrics) returns (Empty) {}

}

message JobMetrics {
    string clientIP = 1;
    int32 cpuCount = 3;
    int32 memoryCount = 4;
}

message Empty {}

